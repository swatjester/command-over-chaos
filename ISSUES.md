# Known Issues

| ID | Sev | Area | Description | Status | Planned fix |
|---|---|---|---|---|---|
| I-001 | high | session/netcode | Refreshing the browser spawns a brand-new squad and orphans the old one (server treats every ws connection as a new player; orphaned soldiers are never reaped). | **fixed** — session tokens (sessionStorage, see I-003) reclaim the squad on reconnect; orphaned squads reaped after 120s grace | Binds to real accounts in M3. |
| I-002 | low | balance | Farmstead courtyard scenario shows a north-side positional advantage (~58/42 over 200 harness battles) — A* route asymmetry through the courtyard gaps. Competitive side swaps neutralize it in matches. | open — re-measured 2026-07-08 after vault links landed: 56/44 over 800 battles, engine team-fair (391/409); vaulting slightly reduced the skew | Symmetric tie-breaking in A* or courtyard gap adjustment; re-measure with tools/balance/mirror-battle.mjs before ranked. |
| I-003 | med | session/netcode | Second browser window to localhost:5173 reclaimed the existing squad instead of joining as a new player (I-001 token was in localStorage, shared across windows). Blocked local multiplayer testing. | **fixed** (f392539) — token moved to sessionStorage: per-tab identity, still survives F5 so I-001 refresh-reclaim is preserved | Real accounts in M3 replace token scheme. |
| I-004 | med | readability/UX | Playtest (replay match-1783133510814, courtyard-wall vs building fight ~t364–385s) FELT like one-way visibility: attackers wiped defenders "without return fire." Replay forensics: fight was fully mutual and fair — defenders fired first and more (33 shots vs 28), landed 4 hits (nominal vs 4.6 expected); attackers ran hot (9 hits vs 5.1 expected, ~6% tail); LOS/fog/RNG all verified symmetric. Root cause is READABILITY: 0.25s thin tracers, no muzzle flash, miss offsets ≤1.5m invisible at 47m, no under-fire indicator — outgoing hits register emotionally, incoming misses don't register at all. Secondary: flat ×0.50 target-in-cover gives building-window defenders zero edge over attackers standing near a low wall. | **fixed** (2026-07-08) — (a) muzzle flash, fat beam tracers (zoom-independent, hits linger), red damage flash on victims, ⚠ UNDER FIRE chip + card glow for 2s after any incoming shot; (b) cover-quality tiers by obstacle kind: window ×0.40, wall/stone/shed ×0.50, hay/tree ×0.55, fence ×0.65 — strongest intervening cover near the target wins, so window defenders out-cover attackers at low freestanding walls | Cover-tier values are placeholder — sweep with the balance harness before ranked. |
| I-005 | high | balance/engine | The mirror harness's courtyard scenario shows a **reproducible ~43/57 team skew that follows soldier-ID slots exactly** (swapping which team spawns first flips 131/167 to 167/131 over the same 300 seeds) since the fire-on-move gate landed. Forensics so far: baseline d7f08a0 fair (295/305@600); gate alone reproduces it even with all tuning numbers reverted; PRNG clean (positional volley test 24.01/24.03%, autocorr <1e-4); stationary mixed-kit duels fair; empty-map marches fair; A* arrivals mirror-symmetric; pre-first-blood shot counts AND rolled-pct identical (2441/2441 @ 24.2/24.2) yet first casualty lands on the low-id team 62/38. Suspected: gate + suppression-pin feedback + courtyard-funnel geometry amplify an unidentified id-ordered micro-asymmetry in the tick pipeline. Pip kill-credit id-priority was found and fixed during the hunt (credit now shared on simultaneous kills) — didn't change the aggregate. | open — **must resolve before ranked**; fine for feature playtesting (real matches are position-asymmetric anyway) | Next probes: per-tick suppression asymmetry during the approach; instrument first-blood volley composition; try reversed-id iteration in the shooter loop (if the skew flips, the pipeline order is the culprit); consider randomized (seeded) shooter iteration order per tick. |
