# tools/balance (stub — M1+)

Headless sim-vs-sim harness: runs thousands of automated engagements across archetype matchups per balance-data change (packages/shared/src/fireteams.json). Outputs win-rate matrices per patch. See PLAN.md §8.3.
